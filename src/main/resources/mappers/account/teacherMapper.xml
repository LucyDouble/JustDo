<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="Teacher">
	<resultMap type="Teacher" id="TeacherMap">
		<result property="teacher_number" column="TEACHER_NUMBER"/>
		<result property="id" column="ID"/>
		<result property="password" column="PASSWORD"/>
		<result property="teacher_name" column="NAME" />
		<result property="teacher_address" column="ADDRESS" />
		<result property="teacher_phone" column="PHONE" />
		<result property="teacher_email" column="EMAIL" />
		<result property="accept" column="ACCEPT"/>
		
	</resultMap>
	


	<select id="loginCheck" resultType="String">
		SELECT COUNT(*) FROM TEACHER 
		WHERE ID=#{id} AND PASSWORD = #{password}
	</select>
	<select id="idCheck" resultType="int">
		SELECT COUNT(*) FROM TEACHER 
		WHERE id = #{id} 
	</select>
	<select id="signUp">
		insert into teacher(teacher_num, id, password, teacher_name, teacher_address, teacher_phone, teacher_email) 
		values (#{teacher_num}, #{id}, #{password}, #{teacher_name}, #{teacher_address}, #{teacher_phone}, #{teacher_email}, #{accept})
	</select>
	<select id="infoTeacher" parameterType="Teacher" resultMap="TeacherMap">
			SELECT * FROM TEACHER
			WHERE ID = #{id} AND PASSWORD = #{password}
	</select>

</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="Student">
	<resultMap type="StudentDTO" id="StudentMap">
		<result column="student_num" property="student_number"/>
		<result column="student_id" property="id"/>
		<result column="student_pwd" property="password"/>
		<result column="student_name" property="name"/>
		<result column="student_address" property="address"/>
		<result column="student_phone" property="phone"/>
		<result column="student_email" property="email"/>
	</resultMap>
	<select id="loginCheck" resultType="int">
		SELECT COUNT(*) FROM STUDENT 
		WHERE student_num = #{student_number} AND student_pwd = #{password}
	</select>
	<select id="idCheck" resultType="int">
		SELECT COUNT(*) FROM TEACHER 
		WHERE student_id = #{id} 
	</select>
	<select id="signUp">
		insert into student(student_num, student_id, student_pwd, student_name, student_address, student_phone, student_email) 
		values (#{student_number}, #{id}, #{password}, #{name}, #{address}, #{phone}, #{email})
	</select>

</mapper>
